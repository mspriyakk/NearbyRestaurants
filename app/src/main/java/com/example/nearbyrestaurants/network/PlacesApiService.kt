package com.example.nearbyrestaurants.network
/*
private const val BASE_URL = "https://maps.googleapis.com/maps/api/place/"
interface PlacesApiService {
    @GET ("nearbysearch/json") suspend fun getPlaces(
        @Query("location") location: String,@Query("radius") radius: Int,@Query("type") type: String,@Query("key") apikey: string):Response
    private val json =Json{
        ignoreUnknownKeys = true}
    private val retrofit = Retrofit.Builder()
        .baseUrl(BASE URL)
        .addConverterFactory(json.asConverterFactory("application/json".toMediaType())).build()
    object PlacesApi {
        val retrofitService :PlacesApiService by lazy {
            retrofit.create(PlacesApiService::class.java)
        }

 */